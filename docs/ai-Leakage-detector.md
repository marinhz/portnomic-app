Technical Specification: AI Leakage Detector (Automated Expense Audit)1. ObjectiveTo automatically cross-reference incoming vendor invoices (DA line items) against actual operational data (Port Call logs) to identify overbilling, duplicate charges, or tariff misapplications.2. System Workflow2.1 Trigger & IngestionSource: The module triggers when an email containing a financial document is processed by the ARQ background worker.Context Retrieval: The system fetches the corresponding PortCall record (ATA, ATD, Berthing time) and Vessel dimensions (GT, LOA).2.2 Dual-Prompt Processing (BYOAI)The system uses the tenant-configured LLM to perform a two-step audit:Extraction: AI extracts structured line items from the invoice (Service, Quantity, Unit Price, Date/Time of service).Validation: AI compares the extracted service timestamps against the AuditLog and PortCall operational events.3. Key Audit Logic (Detection Rules)Rule IDDetection LogicData Points RequiredLD-001Temporal ValidationCompares invoice service date vs. Vessel stay (ATA/ATD).LD-002Duplicate DetectionChecks DA entity for identical service descriptions/amounts in the same PortCall.LD-003Tariff Shift AuditFlags "Weekend/Holiday" rates if the PortCall log shows the service occurred during standard hours.LD-004Quantity VarianceCompares invoiced tug/pilot hours vs. AI-parsed Noon Reports.4. Technical Requirements4.1 Backend Implementation (FastAPI & Python)Service Layer: Create an AuditService that handles the comparison logic between ai_raw_output and PortCall state.Anomaly Persistence: Detected discrepancies must be stored in a new Anomaly table linked to the Email and DA entities.Circuit Breaker: Ensure LLM failures do not halt the core ingestion flow; if the audit fails, the invoice is marked "Pending Manual Review".4.2 Frontend Integration (React & shadcn/ui)Status Indicators: Use Sonner toasts to alert the agent of "High Risk" invoices immediately upon parsing.Discrepancy UI: A side-by-side view in the DA Workspace showing the "Invoiced Value" vs. "System Expected Value".Highlighting: Visual flags on line items that failed the audit logic.5. Security & Privacy (BYOAI Strategy)API Key Routing: The module must use the tenant_id to retrieve the encrypted LLM key from the Tenant LLM config.Audit Transparency: All AI-driven audit decisions are logged in the append-only AuditLog for accountability.Why this is a "Killer Feature" for Investors:Zero Infrastructure Overhead: You don't pay for the heavy lifting; the client's own AI key does the work.Immediate ROI: The system doesn't just "manage" data; it actively saves money by catching human error or vendor overcharging.Data Synergy: It utilizes every part of your stack—Vessel data, Email parsing, Financial workflows, and Audit logs.Would you like me to generate the SQL schema for the Anomaly table or the specific LLM prompt for the Leakage Detection logic?
